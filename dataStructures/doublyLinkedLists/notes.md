just liked singly linked lists except you now have a pointer to the previous node as well as the next node

doubly linked lists have more flexibility than singly linked lists but require more memory

Big O:

- insertion - O(1)
- removal - O(1)
- searching - O(n)
- access - O(n)

