// traversing a tree without repeating values can be done with 2 main methods

// breadth-first search
// visiting every node on the same level first before moving on to the next level

class tree {

}



const bfs = arr => {
    // create a queue (can be an array) and
    // a variable to store the values of nodes visited
    // place the root node in the queue
    // loop as long as there is anything in the queue
        // dequeue a node from the queue and push the value of the node into the variable that stores the nodes
        // if there is a left property on the node dequeued
            // add it to the queue
        // if there is a right property
            // add it to the queue
    // return the variable that stores all of our values
}


// depth-first search
