// novice version
function addUpTo(n) {
  let total = 0;
  for (let i = 0; i <= n; i++) {
    total += i;
  }
  return total;
}

// nerdy verdy
function addDatShit(n) {
  return (n * (n + 1)) / 2;
}

// the nerdy verdy is a lot faster